<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ShoppingList" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/orange</item>
        <item name="android:windowBackground">@color/yellow</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppBarStyleHome" parent="Theme.ShoppingList">
        <item name="actionOverflowMenuStyle">@style/AppBarStyle</item>
    </style>

    <style name="AppBarStyle" parent="Theme.ShoppingList">
        <item name="android:popupBackground">@drawable/rounded_background</item>
        <item name="android:width">50dp</item>
    </style>

</resources>